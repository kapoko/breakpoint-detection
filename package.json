{
  "name": "breakpoint-detection",
  "version": "0.1.4",
  "description": "Pure Javascript breakpoint detection for Bootstrap",
  "main": "dist/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/kapoko/breakpoint-detection"
  },
  "author": "Kasper Koman <kasper.koman@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  },
  "scripts": {
    "build": "babel src --out-dir dist",
    "watch": "babel src --watch --out-dir dist",
    "prepare": "yarn run build"
  },
  "files": [
    "dist"
  ]
}
